{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://polder.dev/schemas/frontmatter.schema.json",
  "title": "Vault note frontmatter",
  "description": "Canonical structural contract for vault note frontmatter (AUDIT.md \u00a740, \u00a7P1 exit criterion). Type and status vocabularies are exported by src/polder_research/paths and verified alongside this schema; this document only enforces structural shape so the canonical vocabulary is not duplicated.",
  "type": "object",
  "required": ["type", "status", "tags"],
  "additionalProperties": true,
  "properties": {
    "type": {"type": "string", "minLength": 1},
    "status": {"type": "string", "minLength": 1},
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
      }
    },
    "created": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"},
    "updated": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"}
  }
}
